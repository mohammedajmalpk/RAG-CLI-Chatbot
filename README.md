📘 RAG-CLI-Chatbot

A simple and powerful CLI-based RAG (Retrieval-Augmented Generation) chatbot that reads PDFs, creates embeddings using HuggingFace Sentence-Transformers, stores vectors in FAISS, and answers user questions using semantic search.

 - 🚀 Features 
 - 📄 Extract text from PDF files 
 - ✂️ Smart text chunking
 - 🔍 Create embeddings using all-MiniLM-L6-v2 
 - 🧠 Store vectors in FAISS (CPU)
 - ❓ Ask queries and retrieve top matching chunks 
 - 🤖 Generate answers using LLM 
 - 💻 Fully CLI-based (no UI required)